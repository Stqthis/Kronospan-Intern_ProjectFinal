

PyQt6           # desktop GUI
faiss-cpu>=1.7.4       # vector similarity search (CPU build)
PyMuPDF>=1.23.0        # PDF text extraction (imported as `fitz`)
pandas>=2.0.0          # Excel ingestion + DataFrame handling
openpyxl>=3.1.0        # .xlsx engine used by pandas.read_excel
matplotlib>=3.7.0      # plotting (Agg backend inside the sandbox)
numpy>=1.24.0          # arrays / embedding matrices
requests>=2.31.0       # talks to the local Ollama HTTP API

